<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ page_title }}</title>
    {{ content_for_header }}
    {{ 'variant-selector.css' | asset_url | stylesheet_tag }}
    <script src="{{ 'variant-picker-dropdown.js' | asset_url }}" defer></script>
  </head>
  <body>
    {{ content_for_layout }}
  </body>
</html>